#include"main.h"
/**
 * _abs - gives absolute value
 * of an integer
 *
 * @n: input number as an integer.
 *
 * Return - absolute value
 */
int _abs(int);
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n* -1);
	}
}
